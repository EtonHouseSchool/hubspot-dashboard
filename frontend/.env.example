# Used by the /api serverless functions (via `vercel dev` for local testing).
# In production, set this in Vercel's Project Settings -> Environment
# Variables instead -- this file is only a local template, never commit
# your real token.
HUBSPOT_TOKEN=pat-na1-xxxxxxxxxxxxxxxxxxxx
