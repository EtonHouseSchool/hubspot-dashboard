{
  "name": "hubspot-dashboard-backend",
  "version": "1.0.0",
  "description": "Fetches deal data from HubSpot, aggregates it by stage and time window, and serves it to the dashboard frontend.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "node-cron": "^3.0.3"
  }
}
